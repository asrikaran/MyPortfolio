import React from "react";
import ReactDOM from "react-dom";
import App from "./App";  // Ensure correct import of App
import "./styles/index.css";  // Ensure correct import of CSS

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
